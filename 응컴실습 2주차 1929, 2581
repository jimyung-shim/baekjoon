// 1929번
#include<stdio.h>
int main()
{
	int n1, n2;
	scanf_s("%d %d", &n1, &n2);

	for (int i = n1; i <= n2; i++) {
		int flag = 1;
		for (int j = 2; j < i; j++) {
			if (i % j == 0)flag = 0;
		}

		if (flag)printf("%d\n", i);
	}
	return 0;
}

// 2581번
#include<stdio.h>
int main()
{
	int n1, n2;
	scanf_s("%d %d", &n1, &n2);

	int sum = 0;
	int fst = 1;
	int min = 0;

	for (int i = n1; i <= n2; i++) {
		int flag = 1;
		for (int j = 2; j < i; j++) {
			if (i % j == 0)flag = 0;
		}

		if (flag) {
			if (fst == 1) {
				min = i;
				fst = 0;
			}
			sum += i;
		}
	}


	if (fst == 1)printf("-1");
	else printf("%d\n%d", sum, min);

	return 0;
}
