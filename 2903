#include<stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    int sum=2;
    int i;
    for(i=0;i<n-1;i++){
        sum+=(sum-1);
    }
    int d;
    d=(sum+(sum-1))*(sum+(sum-1));
    printf("%d", d);
    return 0;
}
